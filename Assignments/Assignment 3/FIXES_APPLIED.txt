=============================================================================
FIXES APPLIED TO "Supervised Learbing Modelling.R"
=============================================================================

1. LIBRARY LOADING
   - Added: library(forcats)
   - This was missing but required for fct_collapse() function

2. FACTOR LEVEL MERGING (Lines ~55-64)
   FIXED: Updated factor collapsing to match actual data levels:
   
   Source.OS.Detected:
   - Added Linux_All = c("Linux (>4.0)", "Linux (2.6.3)")
   - Kept Windows_All = c("Windows 10", "Windows Server 2008")
   
   Target.Honeypot.Server.OS:
   - Changed to Unix_Like = c("Linux", "MacOS (All)")
   - Changed to Windows_All = c("Windows (Desktops)", "Windows (Servers)")
   
   REASON: Original code used non-existent category names

3. APT FACTOR CONVERSION (Lines ~76-79)
   ADDED: Explicit factor conversion and distribution check
   - dat_cleaned$APT <- factor(dat_cleaned$APT, levels = c("No", "Yes"))
   - Added diagnostic output for APT distribution
   
   REASON: Ensures proper classification target format

4. BAGGING PARAMETERS (Lines ~192-210)
   FIXED: Added as.numeric() conversions for grid parameters:
   - nbagg = as.numeric(params$nbagg)
   - cp = as.numeric(params$cp)
   - minsplit = as.numeric(params$minsplit)
   - Added diagnostic message: "Best Bagging Parameters:"
   
   REASON: expand.grid returns data.frame columns which can cause type issues

5. RANDOM FOREST PARAMETERS (Lines ~233-251)
   FIXED: Added as.numeric() conversions for grid parameters:
   - ntree = as.numeric(params$ntree)
   - mtry = as.numeric(params$mtry)
   - nodesize = as.numeric(params$nodesize)
   - params$ntree index conversion in err.rate access
   - Added diagnostic message: "Best Random Forest Parameters:"
   
   REASON: Same as bagging - prevents type coercion errors

=============================================================================
TESTING RECOMMENDATIONS
=============================================================================

1. Verify data loads correctly:
   - Check that WACY-COM.csv is in the same directory
   - Confirm 200,000 initial rows

2. Monitor factor levels after merging:
   - Source.OS.Detected should have: Windows_All, Linux_All, MacOS (Unknown)
   - Target.Honeypot.Server.OS should have: Windows_All, Unix_Like

3. Check cleaned data dimensions:
   - Should have ~199k+ rows after cleaning
   - Should have 12-13 columns after transformations

4. Verify model training:
   - All models should train without errors
   - Cross-validation should complete
   - Predictions should match factor levels of APT

=============================================================================
